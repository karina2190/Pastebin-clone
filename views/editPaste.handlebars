<!-- În fișierul editPaste.handlebars -->

<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Paste</title>
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Edit Paste {{paste.id}}</h1>
        <form action="/updatePaste/{{paste.id}}" method="post">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="form-control" id="title" name="title" value="{{paste.title}}">
            </div>
            <div class="form-group">
                <label for="content">Content</label>
                <textarea class="form-control" id="content" name="content" rows="5">{{paste.content}}</textarea>
            </div>
            <button type="submit" class="btn btn-primary">Update paste</button>
        </form>
    </div>
</body>
</html>
